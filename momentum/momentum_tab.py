"""Momentum tab -- placeholder until the momentum strategy is built."""

import streamlit as st


def render():
    st.header("Momentum \u2014 US Treasury ETFs")
    st.info(
        "Not built yet. Momentum will use the trailing 12-month return of each "
        "maturity as its signal, following the same style-investing framework "
        "as Carry."
    )
